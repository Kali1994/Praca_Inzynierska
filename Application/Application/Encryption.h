#pragma once
#include "stdafx.h"
#include "Picture.h"
#include "Extract.h"

class Encryption : public Extract
{
public:
	/*
	Encryption(Picture*);

	uint8_t* u8ComputingLimitsOnScrambling(int,int,int);

	void vTransposingPixelColor(uint8_t*, uint8_t*,int,int,int);
	void vTransposingPixelColorDescrambling(uint8_t*, uint8_t*,int, int, int);
	
	~Encryption();
private:
	int m_iOldi;
	int m_iOldj;
protected:
	Picture* m_piPicture;
	*/
};
